<header class="site-header">
  <div class="site-title"><a href="{{ "/" | relative_url }}">{{ site.title }}</a></div>
</header>

<main class="content">
  {% if page.title %}
    <h1>{{ page.title }}</h1>
  {% endif %}
  {{ content }}
</main>
